Yes, you are exactly right about the distinction between layers.UI/Application Level AttributesThe attributes you mentioned belong entirely to your front-end user interface or your application middleware layer, not inside the LLM system prompt.Language Options: Handled at the UI level by providing translation drop-downs, or by letting the app detect the browser locale and passing a language override flag to the LLM.Session Time: Handled by your application web-sockets or API gateway session timeouts to reset the chat container if a user walks away.System/Device Attributes: Values like the user's current geographic location, device type, or dark/light mode preference are managed natively by the client browser.What Can Be Added to the System Prompt (AI Behavior Level)Apart from operational hours and party size caps, you can add several strict behavioral guardrails directly into the Amazon Bedrock System Prompt to control exactly how the LLM manages the conversation logic:1. Temporal Constraints (Handling Relative Dates)The Problem: If a customer says "Book me a table for tomorrow" or "next Friday", the LLM has no innate concept of what day "today" is.The Prompt Fix: Add an instruction telling the agent to cross-reference a dynamic environment variable $CURRENT_DATE passed in the session context, ensuring it maps relative time phrases to absolute dates (YYYY-MM-DD) accurately before calling the Lambda.2. Ambiguity Resolution (Disambiguation)The Problem: If findBookingByName returns multiple reservations for the same name, the LLM might panic or dump a messy list.The Prompt Fix: Guide the LLM on how to present options: "If multiple bookings are returned for a single name, present them as a numbered list and ask the customer to select the correct date and time."3. Strict Boundary/Out-of-Scope GuardrailsThe Problem: Users might try to treat your reservation assistant like a general-purpose chatbot, asking it to write code, discuss politics, or recommend recipes.The Prompt Fix: Add a hard constraint block: "You are strictly a restaurant booking agent. If the user asks questions unrelated to R-Cafe reservations, menus, or hours, politely redirect them back to booking a table."4. Booking Modification SafetyThe Problem: A user might try to change a booking without proving it belongs to them.The Prompt Fix: Enforce an authorization check step in the text: "Never execute updateBooking or deleteBooking unless the customer provides BOTH the matching Booking ID and the primary customer name registered to that slot."Would you like to expand the system prompt to include the Relative Date Mapping logic, or would you like to see how to pass Session Attributes dynamically into your Amazon Bedrock Agent text stream?You said: If we can distinguish what attributes can go into system prompt and Ui level and other ways.To build a clean, stable system, it is vital to separate your architecture into distinct layers. Mixing up where data lives causes bugs. For example, if you hardcode today’s date in a system prompt, it will quickly become outdated and invalid.Here is a clear architectural breakdown of where attributes must reside across your system.1. UI & Client Application LevelThis layer handles the user's interface, local environment, and session state. It captures parameters from the browser or device and passes them down to the backend.Session Timeouts: Tracks user inactivity. It clears the chat screen after 15 minutes of idle time.Language Selection: The visual toggle menu (e.g., English, Spanish) that triggers your app's localization files.Theme & Accessibility: Dark mode, text-to-speech preferences, and font sizing.Device Attributes: Collecting screen resolution, OS version, or camera access permission.Geographical Location: Capturing the physical GPS coordinates of the user device (with permission) to see which restaurant branch is closest.2. Session Context Level (Dynamic Metadata Pipeline)This is the middleware layer that sits between your UI and your Bedrock Agent. It intercepts the chat and passes vital, changing background data to the LLM behind the scenes.Dynamic Current Timestamp: Automatically injects the precise current date and time (e.g., Current Date: Monday, June 22, 2026) into the header of every API call. This is how the LLM understands what "tomorrow" means.Authentication Token / User ID: Securely routes the logged-in customer's profile token to verify their identity without forcing them to type their credentials into the chat window.3. System Prompt Level (AI Logic & Constraints)This lives strictly inside Amazon Bedrock. It controls the thinking process, behavior, and conversational rules of the AI.Relative Date Processing Rules: Instructions telling the LLM to convert phrases like "this weekend" into an absolute YYYY-MM-DD format using the session timestamp.Disambiguation Logic: Clear steps on what to do if a search pulls up multiple results. For example: "If a name lookup returns three bookings, display them as a numbered list and ask the customer to clarify."Conversational Guardrails: Explicit boundaries that keep the AI on track. For instance: "Politely refuse to answer questions about coding, politics, or unrelated topics."Business Policy Constraints: Setting structural limits, such as a maximum party size cap of 12 guests per table reservation.4. Lambda & Database Layer (Backend Validation)This is your final line of defense. The LLM should never be trusted blindly with business calculations or secure operations.Database Schema Mapping: Tracking the physical structure of your DynamoDB primary composite keys (Booking_ID and Booking_DateTime).Operational Capacity Checks: Running live database queries to verify if the restaurant actually has a physical table open at 7:00 PM before confirming.Strict Operational Hours: Hardcoded ranges in your Lambda script to reject bookings made outside operational windows, throwing an explicit error back if violated.Proactively planning this separation ensures your system remains scalable. If you're ready, we can draft a updated version of your Amazon Bedrock System Prompt that explicitly leverages Session Context Attributes for flawless relative date calculations. Would you like to proceed with that?

What Changed? (Improvements from Last Version)Integrated Your GSI Setup: Converted findBookingByName from a resource-heavy .scan() to a highly performant .query() leveraging your new index configuration.Operational Barrier Validation: Added defensive checks right inside the Lambda function (validate_reservation) to reject bookings if they fall outside restaurant operating hours (11:00 AM to 11:00 PM).Hard Party Capacity Constraints: The code now automatically drops booking requests if the user attempts to reserve a table for more than 12 guests.Past Timestamp Prevention: Added logic to verify dates before running database commands, blocking customers from creating reservations for dates that have already passed.Update Validation Coverage: Fixed updateBooking so that any modifications made to an existing date, time, or size are evaluated against your business rules before the change is applied to DynamoDB

